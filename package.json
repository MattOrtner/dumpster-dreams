{
  "name": "dumpster-dreams",
  "version": "1.0.0",
  "private": true,
  "repository": "https://github.com/W00ST/dumpster-dreams",
  "license": "UNRELEASED",
  "scripts": {
    "start": "./node_modules/.bin/nf start",
    "deploy": "yarn workspace @app/client build && yarn workspace @app/api setup"
  },
  "workspaces": {
    "packages": [
      "src/*"
    ]
  },
  "devDependencies": {
    "foreman": "^3.0.1"
  }
}
